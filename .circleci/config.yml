version: 2.1
jobs:
  build:
    docker:
      - image: apon77/aosp:cirrus
    steps:
      - checkout
      - run:
          name: "Sync"
          command: ./clone.sh
      - run:
          name: "Build"
          command: ./build.sh
